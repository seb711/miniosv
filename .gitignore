*~
build/
.\#*
\#*\#

.venv
# cloud-deploy runtime state (snapshot/image/vm ids), never tracked
/aws/
/gcp/
.gradle
tags
TAGS

cscope.in.out
cscope.out
cscope.po.out

*.pyc
*.swp
*.patch

*.orig
*.rej

# file generated by trace tool
tracefile
tracefile.symbols

build.out
build.*.log

run.log

.cache
.idea
compile_commands.json
downloaded_packages
.firecracker
external/llvm-project/
aws

# The application lives outside this repo. app/ is populated by cloning the
# miniduckdb fork (https://github.com/Martin-Lndbl/miniduckdb) into it — see
# that repo's README for setup. Nothing under app/ is tracked here.
/app/
