# Hello-world reference app for miniOSv.
#
# The kernel's top-level Makefile includes this fragment (via `include
# app/Makefile`) after the app source tree has been staged at app/. It just
# has to declare $(app-objects); the kernel compiles those with its own
# flags and links them into loader.elf.

app-objects = app/app.o
